---
import HeartIcon from '@tabler/icons/outline/heart.svg';

import {
  getFaviconSrc,
  INSPIRATIONAL_SITES,
  TOOLS_USED,
} from '@/data/tools-and-inspirations';

import Button from './starwind/button';
import { Popover, PopoverContent, PopoverTrigger } from './starwind/popover';
---

<Popover class='group/popover'>
  <PopoverTrigger asChild>
    <Button
      variant='ghost'
      chevron={{
        orientation: 'up',
        size: 'md',
      }}
      class='w-fit
        group-data-[state=open]/popover:bg-primary/10
        group-data-[state=open]/popover:text-primary
        hocus:scale-[102%]'
    >
      <HeartIcon />
      <span>Tools & Inspirations</span></Button
    >
  </PopoverTrigger>

  <PopoverContent side='top' aria-label='Tools & Inspirations' class='p-3'>
    <div>
      <div class='mb-1 text-xs font-bold text-muted-foreground'>TOOLS</div>
      <div class='grid gap-1'>
        {
          TOOLS_USED.map(({ name, description, url, favicon }) => (
            <a
              href={url}
              target='_blank'
              rel='noopener noreferrer'
              class='
                flex bouncy-click items-center gap-3 rounded-md p-2
                transition-colors
                hover:bg-secondary
                hocus:scale-[102%]
              '
            >
              <img
                src={getFaviconSrc(favicon)}
                alt=''
                class='size-8 rounded-full'
              />
              <div>
                <div class='text-sm'>{name}</div>
                {description && (
                  <div class='text-xs text-muted-foreground'>{description}</div>
                )}
              </div>
            </a>
          ))
        }
      </div>

      <!-- <Separator class='mx-auto mt-4 mb-6 bg-(--ctp-surface2)' /> -->

      <div class='mt-6 mb-1 text-xs font-bold text-muted-foreground'>
        INSPIRATIONS
      </div>
      <div class='grid gap-1'>
        {
          INSPIRATIONAL_SITES.map(({ name, url, favicon, description }) => (
            <a
              href={url}
              target='_blank'
              rel='noopener noreferrer'
              class='
                flex bouncy-click items-center gap-3 rounded-md p-2
                transition-colors
                hover:bg-secondary
                hocus:scale-[102%]
              '
            >
              <img
                src={getFaviconSrc(favicon)}
                alt=''
                class='size-8 rounded-full'
              />
              <div>
                <div class='text-sm'>{name}</div>
                {description && (
                  <div class='text-xs text-muted-foreground'>{description}</div>
                )}
              </div>
            </a>
          ))
        }
      </div>
    </div>
  </PopoverContent>
</Popover>
